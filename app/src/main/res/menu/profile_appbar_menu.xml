<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", must appear as action button if possible. -->
    <item
        android:id="@+id/action_notifications"
        android:icon="@drawable/baseline_notifications_32"
        android:title="@string/notifications_button"
        app:actionLayout="@layout/notifications_profile_appbar_menu"
        app:showAsAction="ifRoom"/>

    <!-- Settings, must always be in the overflow. -->
    <item
        android:id="@+id/action_settings"
        android:icon="@drawable/ic_baseline_menu_32"
        android:title="@string/settings_button_profile"
        app:showAsAction="ifRoom"/>
</menu>